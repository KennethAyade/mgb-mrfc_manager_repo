# Render Deployment Configuration
# This file tells Render how to build and deploy only the backend folder

# Build Command
build: npm install && npm run build

# Start Command  
start: npm start

# Root Directory (IMPORTANT!)
rootDir: backend

# Environment
NODE_ENV: production

